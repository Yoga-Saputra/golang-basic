package main

import "fmt"

func main() {
	// membuat slice
	// array[low:high] => membuat slice dari array dimulai index low sampai index sebelum high
	// array[low:] => membuat slice dari array dimulai index low sampai index akhir di high
	// array[:high] => membuat slice dari array dimulai index 0 sampai index sebelum high
	// array[:] => membuat slice dari array dimulai index 0 sampai index akhir di high

	fmt.Println()
}
